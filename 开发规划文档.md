# Recall · AI 智能错题本 — 开发规划文档

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.0 |
| 更新日期 | 2026-08-08 |
| 输入依据 | PRD v1.1（PRD.md）+ UI/UX 设计文档 v1.0（设计文档-UIUX.md） |
| 执行者 | 个人开发者（1 人） |
| 总体周期 | 6 周（W1–W6），目标交付可演示 MVP + 种子用户内测 |

---

## 一、开发总纲

### 1.1 目标

在 6 周内完成 Recall MVP 三版本（v0.1 核心闭环 → v0.2 智能增强 → v0.3 打磨上线），达成 PRD 1.3 关键指标（W2 留存 ≥15%、周复习完成率 ≥30%、AI 归类准确率 ≥85%）。

### 1.2 技术栈（与 PRD 一致）

| 层 | 选型 |
|----|------|
| 前端 | Vue3 + TypeScript + Vite + Tailwind CSS + Pinia + Vue Router |
| 后端 | Python 3.10 + FastAPI + Uvicorn + SQLModel（ORM）+ APScheduler |
| 存储 | SQLite（业务数据）+ ChromaDB（向量）+ 本地图片目录 |
| AI | DeepSeek API（归类/变体题/批改/对话）+ PaddleOCR 3.x 轻量版（本地 OCR）+ BGE-M3 本地（embedding） |
| 导出 | ReportLab（PDF）+ 后端拼装 Markdown |

### 1.3 架构策略（5 条）

1. **前后端分离**：Vue3 SPA（5173）↔ FastAPI（8000），开发期 Vite 代理 `/api`；
2. **接口先行**：先按本文档 API 契约定义路由与 Pydantic 模型，再实现业务；
3. **AI 能力服务化**：所有 DeepSeek/OCR/embedding 调用封装在 `app/ai/` 服务层，业务层不直接调 SDK，方便替换与降级；
4. **先闭环后增强**：v0.1 先把"录入→列表→复习→导出"跑通，再叠加看板/对话/冲刺；
5. **降级兜底**：所有 AI 环节提供降级路径（如 AI 归类失败→用户手选；变体题失败→原题复习）。

### 1.4 目录结构（约定）

```
E:\新桌面\ai错题本\
├── frontend/                  # Vue3 前端
│   ├── src/
│   │   ├── api/               # API 封装（与契约一一对应）
│   │   ├── components/        # 通用组件（NavBar/卡片/气泡…）
│   │   ├── composables/       # 组合式函数（useQuestions/useChat…）
│   │   ├── views/             # 页面（对应设计文档 9 页）
│   │   ├── stores/            # Pinia
│   │   ├── types/             # TS 类型（与后端 schema 对齐）
│   │   └── assets/
│   ├── tailwind.config.js     # 设计 Token 映射（颜色/字号/间距）
│   └── vite.config.ts
├── backend/                   # FastAPI 后端
│   ├── app/
│   │   ├── main.py            # 应用入口
│   │   ├── core/              # config/.env 加载/依赖
│   │   ├── models/            # SQLModel 表模型
│   │   ├── schemas/           # Pydantic 请求/响应模型
│   │   ├── api/routes/        # 路由（notebooks/questions/capture/reviews/…）
│   │   ├── services/          # 业务逻辑层
│   │   └── ai/                # deepseek_client / ocr_service / embedding_service
│   ├── data/                  # SQLite + ChromaDB + 图片（运行期生成）
│   └── requirements.txt
├── docs/                      # 本规划 + 评测集（100 道预标注题）
└── UI示例/                    # 设计文档的 HTML/PNG 示例
```

---

## 二、里程碑与任务分解

> 每周末完成可演示版本；任务包按依赖顺序排列；单人串行执行，W2/W4 末尾各留 1 天缓冲。

### 里程碑 M1：环境与脚手架（W1 前半）

| 任务 | 说明 | 验收 |
|------|------|------|
| 1.1 环境准备 | Python 3.10 + Node 18+；`pip install fastapi uvicorn sqlmodel`；`npm create vite` | 前后端可启动，`/api/health` 返回 ok |
| 1.2 **PaddleOCR 安装验证**（最高风险前置项） | `pip install paddlepaddle paddleocr`；验证 CPU 识别一张中文题图耗时 | 单图 ≤5s；失败则启用备选 rapidocr |
| 1.3 **BGE-M3 验证** | 下载模型（或 bge-small-zh 备用），验证 embedding 入库 ChromaDB | 首次向量化可用 |
| 1.4 DeepSeek key 验证 | `.env` 配置，调用一次 chat 与归类 JSON 输出 | 返回稳定 JSON |
| 1.5 脚手架 | 前后端目录、Tailwind 配置（设计 Token 映射）、路由骨架、统一响应格式 | 见 5.4 |

### 里程碑 M2：v0.1 核心闭环（W1 后半 – W2）

| 任务包 | 内容 | 对应 FR | 验收 |
|--------|------|--------|------|
| 2.1 数据模型 | notebooks/questions/review_logs 表 + SQLModel + 迁移 | FR-02/03 | 建表成功，索引生效 |
| 2.2 错题本 API | CRUD + 颜色 | FR-02 | PRD 7.x 全绿 |
| 2.3 文本录入 | 表单 + AI 补全（DeepSeek 归类） | FR-01.2 | 归类可编辑采纳 |
| 2.4 识图录入 | 上传→异步 OCR→AI 拆分→勾选导入（轮询） | FR-01.1 | 7.1–7.4 通过 |
| 2.5 错题列表 | 列表/搜索/筛选/展开编辑 + 虚拟滚动 | FR-03 | 10000 条流畅 |
| 2.6 SM-2 引擎 | repetition/interval/ease/quality + 每日队列 | FR-05.1/5.2 | 7.10 通过 |
| 2.7 导出 | PDF（ReportLab 中文字体）+ Markdown | FR-07 | 7.12 通过 |
| 2.8 UI 页面 | 主页/录入页（对照 UI 示例） | 设计文档 4.1/4.2/4.3 | UI 一致性检查 |

### 里程碑 M3：v0.2 智能增强（W3 – W4）

| 任务包 | 内容 | 对应 FR | 验收 |
|--------|------|--------|------|
| 3.1 一键复习 | ChromaDB 检索 → DeepSeek 变体题（流式）→ 作答 → 批改 | FR-04 | 7.5–7.6 通过 |
| 3.2 数据看板 | KPI/趋势图/知识图谱/分布（ECharts） | FR-06 | 7.11 通过 |
| 3.3 AI 对话 | SSE 流式 + 会话历史 + 加入错题本 | FR-08 | 7.7–7.8 通过 |
| 3.4 语义搜索 | 列表搜索接入 ChromaDB | FR-03.2 | 语义命中测试 |

### 里程碑 M4：v0.3 打磨上线（W5 – W6）

| 任务包 | 内容 | 对应 FR | 验收 |
|--------|------|--------|------|
| 4.1 考前冲刺 | 考试日期 → 考前 N 天队列 | FR-05.4 | 冲刺队列生成正确 |
| 4.2 帮助中心 + 异常全覆盖 | 帮助页 + 异常汇总表 E1–E9 全实现 | FR-09 | 异常用例走查 |
| 4.3 归类评测集回归 | 100 道预标注题跑分 | 7.14 | 三项 ≥85% |
| 4.4 性能调优 | 全量接口压测 + 首屏优化 | 6.1/7.13 | 指标达标 |
| 4.5 种子用户内测 | 100–200 人邀请 + 埋点（录入耗时/复习完成率） | 1.3 | 数据采集上线 |

---

## 三、组件/模块依赖树

### 3.1 前端组件树（Vue3）

```
App
└─ Layout（全局框架）
   ├─ NavBar
   │  ├─ Logo
   │  ├─ NavItems（仪表盘/错题本/复习/看板/AI对话，激活态按路由）
   │  ├─ GlobalSearch（回车→/questions?q=）
   │  └─ EntryButton（→ /capture）
   ├─ NotebookSidebar（260px，仅 /questions、/chat 复用变体 ConversationSidebar）
   │  ├─ NotebookList → NotebookItem（色点+名称+数量+选中态）
   │  ├─ NewNotebookButton
   │  └─ ColorPickerPopover（Teleport 气泡，8 色）
   └─ RouterView
      ├─ DashboardView（仪表盘）
      │  └─ TodayQueueCard / QuickEntries
      ├─ QuestionsView（错题列表）
      │  ├─ FilterBar（SubjectChips / SearchBox / FilterButton）
      │  ├─ QuestionList → QuestionCard（滚动加载哨兵）
      │  │  ├─ QArea（题目区·蓝左边框）/ AArea（解析区·绿左边框·折叠）
      │  │  ├─ Tags（学科/知识点/错因/难度/掌握状态）
      │  │  └─ CardActions（编辑/删除/移动，展开态）
      ├─ CaptureView（识图录入：StepBar + UploadZone + ResultCheckList）
      ├─ InputView（文本录入：QuestionForm + AIClassifySuggest）
      ├─ ReviewView（三阶段）
      │  ├─ StagePicker（范围选择）
      │  ├─ AnswerStage（题目卡 + 进度 n/10）
      │  └─ ResultStage（AccuracyRing + ResultDetailList）
      ├─ DashboardView2（数据看板）
      │  ├─ KpiCards / TrendChart(ECharts) / KnowledgeGraph(ECharts) / DistributionPanels
      ├─ ChatView（双栏）
      │  ├─ ConversationSidebar / MessageList（UserBubble/AiBubble/TypingCursor）/ ChatInput
      └─ HelpView（HelpNav + HelpContent）
```

### 3.2 后端模块依赖树（FastAPI）

```
main.py（CORS + 路由注册 + 统一异常处理）
└─ api/routes/
   ├─ notebooks.py ──────────────→ services/notebook_service ──→ models + SQLite
   ├─ questions.py ──────────────→ services/question_service ──→ models + ChromaDB
   ├─ capture.py ────────────────→ services/capture_service ──→ ai/ocr_service + ai/ai_service
   ├─ reviews.py ────────────────→ services/review_service ──→ ai/ai_service + services/sm2
   ├─ review_plan.py ────────────→ services/plan_service ──→ services/sm2
   ├─ dashboard.py ──────────────→ services/dashboard_service（统计 + 图谱聚合）
   ├─ chat.py ───────────────────→ services/chat_service ──→ ai/ai_service（SSE）
   ├─ export.py ─────────────────→ services/export_service ──→ ReportLab + Markdown
   └─ health.py
core/config.py ← .env（DEEPSEEK_API_KEY / BGE_MODEL_PATH / DATA_DIR）
ai/
   ├─ deepseek_client.py（chat/JSON 归类/变体题/批改，统一超时与重试）
   ├─ ocr_service.py（PaddleOCR 3.x，懒加载单例，异步任务队列）
   └─ embedding_service.py（BGE-M3 懒加载 + ChromaDB 读写）
```

### 3.3 关键依赖约束

- `QuestionCard` 依赖 `NotebookSidebar` 的当前错题本上下文（Pinia store：`useNotebookStore`）；
- `CaptureView` 依赖 `capture_service` 任务队列：上传返回 task_id → 前端轮询 `GET /api/capture/tasks/{id}`（2s 间隔）；
- 所有 AI 调用**必须**走 `ai/` 服务层，禁止在路由/业务层直连 SDK；
- `dashboard_service` 只读统计，不写业务表。

---

## 四、API 契约

### 4.1 统一约定

- 前缀：`/api`；JSON；UTF-8；时间 ISO8601；
- 统一响应：`{ "code": 0, "message": "ok", "data": <payload> }`，`code=0` 成功；
- 错误码：`40000` 参数错误 / `40001` 校验失败 / `40400` 资源不存在 / `40900` 冲突 / `50000` 服务器错误 / `50200` AI 服务超时不可用 / `50300` OCR 识别失败；
- 分页：`?page=1&page_size=20` → `{ items, total, page, page_size }`；
- 认证：MVP 本地单机无鉴权（对外演示时加单用户口令中间件）。

### 4.2 接口清单

#### 错题本
| 方法 | 路径 | 请求 | 响应 data | 说明 |
|------|------|------|-----------|------|
| POST | /api/notebooks | {name, color} | Notebook | 颜色限 8 色枚举 |
| GET | /api/notebooks | - | Notebook[] | 含数量 count |
| PATCH | /api/notebooks/{id} | {name?, color?} | Notebook | |
| DELETE | /api/notebooks/{id} | - | null | 有错题时 409 |

#### 错题
| 方法 | 路径 | 请求 | 响应 data | 说明 |
|------|------|------|-----------|------|
| POST | /api/questions | 文本录入字段 | Question | AI 补全字段可空，用户确认后提交 |
| GET | /api/questions | notebook_id/subject/knowledge_point/error_type/mastery/keyword/page | {items,total} | 关键词含 SQL LIKE |
| GET | /api/questions/search | q | Question[] | ChromaDB 语义搜索 top20 |
| GET | /api/questions/{id} | - | Question | |
| PATCH | /api/questions/{id} | 任意字段 | Question | 更新后同步刷新向量 |
| DELETE | /api/questions/{id} | - | null | 同时删向量与图片 |

#### 识图录入
| 方法 | 路径 | 请求 | 响应 data | 说明 |
|------|------|------|-----------|------|
| POST | /api/capture/upload | multipart(file) | {task_id} | 异步，格式/大小校验 400 |
| GET | /api/capture/tasks/{id} | - | {status, progress, questions?} | processing/done/failed |
| POST | /api/capture/import | {task_id, question_ids} | {imported} | 勾选导入，空列表 40001 |

#### 一键复习
| 方法 | 路径 | 请求 | 响应 data | 说明 |
|------|------|------|-----------|------|
| POST | /api/reviews/generate | {notebook_id?, subject?, count} | SSE 流（逐题变体题） | 首题 <10s；失败降级原题 |
| POST | /api/reviews/{review_id}/submit | {answers:[{question_id, answer}]} | {score,total,correct,results[]} | 未作答按错计 |
| GET | /api/reviews/history | page | {items,total} | 复习记录 |

#### 复习计划（SM-2）
| 方法 | 路径 | 请求 | 响应 data | 说明 |
|------|------|------|-----------|------|
| GET | /api/review-plan/daily | date? | {due: Question[]} | 今日到期 |
| POST | /api/review-plan/complete | {question_id, quality(0-5)} | {next_review_at} | 更新 SM-2 状态 |
| POST | /api/review-plan/exam | {exam_date} | {plan: [{date,count}]} | 考前 N 天队列 |
| GET | /api/review-plan/weekly | - | {summary} | 周汇总 |

#### 数据看板
| 方法 | 路径 | 请求 | 响应 data | 说明 |
|------|------|------|-----------|------|
| GET | /api/dashboard/overview | - | {total, due_today, week_accuracy, streak} | |
| GET | /api/dashboard/trend | days=7/30 | [{date, collected, reviewed, accuracy}] | |
| GET | /api/dashboard/knowledge-graph | - | {nodes[], links[]} | 节点含 mastery |
| GET | /api/dashboard/distributions | - | {subjects[], error_types[]} | |

#### AI 对话
| 方法 | 路径 | 请求 | 响应 data | 说明 |
|------|------|------|-----------|------|
| POST | /api/chat/stream | {conversation_id?, message} | SSE 流 | 首字 <2s |
| GET | /api/chat/conversations | - | Conversation[] | 含最后消息摘要 |
| GET | /api/chat/conversations/{id}/messages | - | Message[] | |
| DELETE | /api/chat/conversations/{id} | - | null | |
| POST | /api/chat/conversations/{id}/add-question | {message_id} | Question | 结构化提取并入库（FR-01.3） |

#### 导出 / 系统
| 方法 | 路径 | 请求 | 响应 data | 说明 |
|------|------|------|-----------|------|
| GET | /api/export/pdf | notebook_id?/subject? | 文件流 application/pdf | 分栏排版+中文字体 |
| GET | /api/export/markdown | notebook_id?/subject? | text/markdown | |
| GET | /api/health | - | {status, version} | 健康检查 |

### 4.3 关键数据结构（Pydantic/SQLModel 对齐）

```python
class Question(Base):
    id: int; notebook_id: int | None
    subject: str            # 枚举：语文/数学/英语/物理/化学/生物/政治/历史/地理/专业课/其他
    knowledge_point: str
    error_type: str         # 枚举：概念不清/审题失误/粗心/计算错误/方法不当/超纲/其他
    difficulty: str         # 易/中/难
    question_text: str; answer: str; analysis: str | None
    image_path: str | None
    mastery_level: int      # 0-5，由 SM-2 quality 派生
    next_review_at: datetime | None
    created_at: datetime
```

---

## 五、开发规范

### 5.1 代码规范

| 项 | 规范 |
|----|------|
| 前端 | ESLint + Prettier；组合式 API（`<script setup>`）；组件 PascalCase；composable `useXxx`；样式只用 Tailwind utility，禁止自定义 css 类（除动画） |
| 后端 | Ruff + Black；路由只做参数校验与响应，业务进 services；Pydantic 模型禁止直接透传 SQLModel |
| 命名 | 后端 snake_case；前端 camelCase；数据库表/字段 snake_case；枚举用字符串常量（英文 key） |
| 类型同步 | 后端 schema 与前端 types 手写对齐；必要时用 openapi-typescript 生成一次 |

### 5.2 Git 规范

- 分支：`main`（可演示）+ `feature/*`（每任务包一个分支），单人开发可简化；
- 提交格式：`feat/fix/docs/refactor/chore: 中文简述`；一次提交一个逻辑变更；
- Agent 代提交时在 commit message 末尾附 `Made-with: Proma` trailer。

### 5.3 配置与密钥

- 密钥只存 `backend/.env`（DEEPSEEK_API_KEY、BGE_MODEL_PATH、DATA_DIR），提交 `.env.example`；
- 数据目录 `backend/data/` 进 .gitignore（SQLite/ChromaDB/图片）；
- 日志：Python logging 输出到 stdout + 文件 `logs/app.log`；关键 AI 调用记录耗时与 token 用量（成本核算用）。

### 5.4 统一响应与异常（与 PRD 异常汇总表 E1–E9 对齐）

- 后端：全局异常处理器，业务异常抛 `AppError(code, message)`；
- 前端：`api/request.ts` 统一拦截——`code!=0` 弹 toast（文案用异常汇总表）；网络失败走 E7 红条；
- 异步任务（OCR）：队列 + 状态轮询；失败记录原因供重试。

### 5.5 UI 一致性

- Tailwind 配置直接映射设计文档 Token（颜色 8 色/字号/间距/圆角/阴影=无）；
- 组件验收对照 `UI示例/*.png`，逐像素核对关键页面；
- 新颜色/字号必须先从设计文档 Token 取，禁止随意硬编码。

---

## 六、风险矩阵

| # | 风险 | 概率 | 影响 | 等级 | 缓解措施 |
|---|------|------|------|------|---------|
| R1 | PaddleOCR 在 Windows 安装/运行失败（paddlepaddle 依赖兼容） | 高 | 高 | 🔴 | W1 第 1 个验证项；备选 rapidocr（pip 直装）；再备选云 OCR 免费额度 |
| R2 | 手写体识别准确率低 | 中 | 中 | 🟡 | 印刷体优先 + 快速人工修正兜底（FR-01.5）；不阻塞闭环 |
| R3 | BGE-M3 CPU 推理慢/首次下载大 | 中 | 中 | 🟡 | 懒加载 + 首次启动预热；备选 bge-small-zh 量化版 |
| R4 | DeepSeek 变体题输出超时/JSON 格式不稳 | 中 | 高 | 🔴 | 强制 JSON mode + 流式返回；失败降级“原题复习”；评测集回归 |
| R5 | AI 归类准确率 <85% | 中 | 中 | 🟡 | Few-shot 提示词 + 枚举约束（FR-01.6）；100 题评测集持续回归 |
| R6 | ChromaDB 数据损坏/版本升级不兼容 | 低 | 中 | 🟡 | 数据目录定期备份；提供“从 SQLite 重建向量”脚本 |
| R7 | SQLite 并发写（ChromaDB 同步写） | 低 | 低 | 🟢 | 单用户足够；写入走服务层串行；必要时上写锁 |
| R8 | 中文 PDF 字体缺失/乱码 | 低 | 低 | 🟢 | ReportLab 注册系统 simhei.ttf；导出用例含中文验证 |
| R9 | 6 周工期压力（单人） | 中 | 高 | 🔴 | 版本内再砍：v0.1 只保证核心闭环；AI 对话降级为 v0.3 可弃项 |

---

## 七、验收标准

### 7.1 版本验收映射（PRD 7.x）

| 版本 | 功能验收（GIVEN-WHEN-THEN） | 性能验收 |
|------|---------------------------|---------|
| v0.1 | 7.1 识图录入 / 7.2 OCR 失败 / 7.3 格式校验 / 7.4 空勾选 / 7.10 SM-2 / 7.12 导出 | 7.13 常规 API <500ms、OCR ≤5s、首屏 <2s |
| v0.2 | 7.5 复习出题批改 / 7.6 未作答 / 7.7 对话流式 / 7.8 空提交 / 7.11 看板 | 7.13 流式首字 <2s、变体题首题 <10s |
| v0.3 | 7.9 网络中断 / 7.14 归类准确率 ≥85% / 异常汇总表 E1–E9 走查 | 全量指标复测 |

### 7.2 UI 一致性验收（对照设计文档第十章完成条件）

- [ ] Tailwind Token 与设计文档 2.1–2.5 完全一致（颜色/字号/间距/圆角）；
- [ ] 主页/答疑页/复习结果页/看板 与 `UI示例/*.png` 结构一致（双栏 260px、卡片分区、边框语义色）；
- [ ] 空状态 4 场景、加载 4 类、错误 6 场景均有实现；
- [ ] 响应式三断点（≥1200 / 768–1199 / <768）行为符合设计文档第九章。

### 7.3 数据验收

- 10000+ 错题回归：造数脚本灌 1 万条 → 列表分页/搜索/看板正常，首屏 <2s；
- 评测集：docs/eval_set.json（100 题，学科/知识点/错因标注）→ 归类三项准确率 ≥85%。

---

## 八、开发节奏建议

### 8.1 每日节奏（工作日 4–6 小时）

| 时段 | 内容 |
|------|------|
| 开工 15min | 对照规划文档，确认今日任务包，重读相关 FR/UI 章节 |
| 编码 3–4h | 单任务包集中开发，接口先行→业务→页面 |
| 收尾 1h | 自测（对应 GIVEN-WHEN-THEN）+ 修复 + commit |
| 每周五 1h | 本周 demo 记录（截图 + 简述）→ 更新规划文档进度 |

### 8.2 关键路径与优先级

```
W1: 环境验证(R1→R3) → 脚手架
W2: 数据模型 → 文本录入 → 识图录入 → 列表 → SM-2 → 导出（v0.1 demo）
W3: 一键复习 → 看板
W4: AI 对话 → 语义搜索（v0.2 demo）
W5: 考前冲刺 → 帮助中心 → 评测集回归
W6: 性能 → 内测部署 → 埋点上线（v0.3 demo）
```

### 8.3 环境准备清单（开工前）

- [ ] Python 3.10 + Node 18+；`git init`（根目录）+ `.gitignore`
- [ ] `pip install fastapi uvicorn sqlmodel chromadb openai`（DeepSeek 兼容）+ `paddlepaddle paddleocr`（R1 验证）
- [ ] BGE-M3 模型目录（或 bge-small-zh 备选）
- [ ] DeepSeek API key → `backend/.env`
- [ ] 拷贝 `UI示例/` 作为前端实现参照；`docs/eval_set.json` 建评测集

### 8.4 节奏要点

1. **先跑通再优化**：v0.1 允许代码不优雅，但闭环必须真实可用；
2. **每天一个可演示增量**：任何任务包完成后都能打开浏览器看到变化；
3. **AI 降级永远可用**：DeepSeek 挂了，产品仍可手动录入+原题复习；
4. **每周留 1 天缓冲**（W2/W4 末尾），风险 R4/R9 超时自动触发砍需求；
5. **成本监控**：日志记录 token 用量，周核对 DeepSeek 花费（目标 5–30 元/月）。

---
